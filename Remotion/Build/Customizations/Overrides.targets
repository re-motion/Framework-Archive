<?xml version="1.0" encoding="utf-8"?>
<Project xmlns="http://schemas.microsoft.com/developer/msbuild/2003">
  <Import Project="$(SolutionDirectory)Build\Remotion.BuildScript.Overrides.targets" />

  <Target Name="BeforePrepareBuildProperties" BeforeTargets="PrepareBuildProperties">
    <PropertyGroup>
      <TempDirectory>$(TempDirectory)Remotion\</TempDirectory>
      <OutputDirectory>$(OutputDirectory)Remotion\</OutputDirectory>
    </PropertyGroup>
  </Target>
</Project>
